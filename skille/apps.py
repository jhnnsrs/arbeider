from django.apps import AppConfig


class SkilleConfig(AppConfig):
    """Skille provides both a Compiler for Graphs and Shares its tasks as seperate Nodes

    Args:
        AppConfig ([type]): [description]
    """
    name = 'skille'
